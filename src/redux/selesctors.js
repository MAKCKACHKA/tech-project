export const selectParfums = state => state.parfum.parfums.items;

export const selectIsLoading = state => state.parfum.parfums.isLoading;

export const selectError = state => state.parfum.parfums.error;
